# NCS - Development profile overrides
logging:
  level:
    com.vnpt.avplatform: DEBUG
    org.springframework.data.mongodb: DEBUG
    org.springframework.kafka: DEBUG

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/av_platform_dev

firebase:
  enabled: false
