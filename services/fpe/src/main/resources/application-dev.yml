# Development profile — local defaults, verbose logging
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/av_platform_dev
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092

security:
  jwt:
    secret: fpe-dev-secret-key-not-for-production-use-32b

logging:
  level:
    com.vnpt.avplatform: DEBUG
    org.springframework.kafka: DEBUG
